apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kafka-app.fullname" . }}-producer-secret
type: Opaque
stringData:
{{- range $k, $v := .Values.producer.secret }}
  {{ $k }}: {{ quote $v }}
{{- end }}
